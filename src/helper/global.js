// env mode flag name
const ENV_DEV_FLAG = `DEV`; // 开发模式
const ENV_STAGE_FLAG = `STAGE`; // 测试模式
const ENV_PROD_FLAG = `PROD`; // 生产模式

// Language flag name
const LANG_CN_FLAG = `zh_CN`; // 简体中文
const LANG_EN_FLAG = `en_US`; // 美式英语

export default {
	ENV_DEV_FLAG,
	ENV_STAGE_FLAG,
	ENV_PROD_FLAG,
	LANG_CN_FLAG,
	LANG_EN_FLAG,
}
