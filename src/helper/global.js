// env mode flag name
const ENV_DEV_FLAG = `DEV`; // 开发模式
const ENV_STAGE_FLAG = `STAGE`; // 测试模式
const ENV_PROD_FLAG = `PROD`; // 生产模式

export default {
	ENV_DEV_FLAG,
	ENV_STAGE_FLAG,
	ENV_PROD_FLAG,
}
